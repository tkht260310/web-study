2026-04-07

- 学習時間：1h

### やったこと

- 配列・for文 2回目（自力実装）

### 詰まったこと・気づき

- for構文は初期化、条件、更新、繰り返す処理を意識して書く
- 配列とは複数の値を入れて管理する入れ物　[]
- 配列の要素はインデックス[番号]で取り出す。[番号]は０始まりがポイント
- 配列の要素を取り出すときの記載要領は、配列名×[]←インデックス
- コミット文を理解＝git -m "（add array←配列）　and （for loop ←繰り返し処理）(practice second implementation 練習２回目の実装）
- git commit-m""のメッセージ内容誤入力
- git commit --amendは直前のコミットを修正するコマンド
- git push --forceは直前のローカルとgithubとの履歴のズレを強制的に上書きするコマンド

### 次回

- 配列・for文 3回目（再実装）
